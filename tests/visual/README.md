# Visual baselines

Captured before the modular refactor from an isolated localhost copy using production data and Chrome at fixed viewports. PNG hashes live in `SHA256SUMS`; exact header and workspace geometry is in `geometry.txt`.

Baselines cover the dashboard, Forge, notifications, metric detail, and maximum-width compressed workspace. Never update these files in the same commit as an unreviewed visual change.
