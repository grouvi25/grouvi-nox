#!/usr/bin/env bash
set -Eeuo pipefail
self_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
if [[ -x "$self_dir/noxctl" ]]; then exec "$self_dir/noxctl" "$@"; fi
if [[ -x /opt/vps-sentinel/bin/noxctl ]]; then exec /opt/vps-sentinel/bin/noxctl "$@"; fi
exec /usr/local/sbin/noxctl "$@"
